# ussd-app

# server logic

# expose server to the internet
```bash
ngrok http http://localhost:3000
```
# get the url and use it to create USSD service on Africa's Talking
that will the call back url

# now you can test it using the simulator on Africa's Talking